<%= form_for([:admin, admin_departament]) do |form| %>
  <% if admin_departament.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_departament.errors.count, "error") %> prohibited this admin_departament from being saved:</h2>

      <ul>
        <% admin_departament.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
